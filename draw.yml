# keymap-drawer/draw.yml
# ❶ どの物理レイアウトで描くか（ここを指定しないとオーソで描かれる）
qmk_keyboard: crkbd/rev1
layout: LAYOUT_split_3x6_3

# ❷ どの YAML（キー割り当て）を描くか
keymap: keymap-drawer/roBa.yaml

# ❸ 出力先（Actions が上書きします）
out: keymap-drawer/roBa.svg

# ❹ お好み（なくてもOK）
draw_config:
  combo_w: 12
  combo_h: 12
  split_gap: 12
